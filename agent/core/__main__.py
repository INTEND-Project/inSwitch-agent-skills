"""Package entrypoint.

Currently a placeholder. The CLI/HTTP bootstrap logic lives in app.py and
will be migrated here in a later commit (see refactor plan).
"""


def main() -> None:
    raise NotImplementedError(
        "core.__main__:main is not yet implemented; use app.py for now."
    )


if __name__ == "__main__":
    main()