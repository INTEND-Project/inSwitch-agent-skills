"""Tool registry and implementations.

Tools are registered here and discovered by the agent runner. Implementations
are split across submodules (shell, files, http, delegation) and will be
populated in a later commit.
"""