---
name: helm-deploy
description: Deploy, upgrade, configure, and delete GATE smart city services using Helm. Use when a lifecycle operation (install, upgrade, rollback, uninstall, value change) must be performed on a smart city Helm release.
---

# Helm Deploy (Placeholder)

> **This skill is a placeholder.** Full instructions will be written in a future iteration.

## Scope

This skill will cover all Helm-based lifecycle operations for GATE smart city services:

- Installing a new service release
- Upgrading an existing release to a new chart version or image tag
- Changing configuration values (`helm upgrade --set` or via `values.yaml`)
- Rolling back a release to a previous revision
- Uninstalling a release

## Status

**To be written.** The parent `gate` skill delegates here for any deploy / delete / upgrade / configure request.
